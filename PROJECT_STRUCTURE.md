# nonebot-plugin-uppic 项目结构说明

## 一、项目概述

本插件是一个基于 NoneBot 2 的随机图片发送插件，支持通过指令触发随机发图、群内添加图片、删除图片、分群管理指令启用/禁用、超级用户权限管理，以及通过阿里云 OSS 或 FastAPI 生成静态网页查看图片。

---

## 二、项目根目录结构

| 文件/目录 | 功能说明 |
|-----------|----------|
| `.github/workflows/pypi-publish.yml` | GitHub Actions 自动发布到 PyPI 的配置 |
| `.idea/` | PyCharm IDE 项目配置文件（开发工具自动生成） |
| `docs/` | 文档截图（预览图、功能演示图等） |
| `nonebot_plugin_uppic/` | **插件核心代码目录** |
| `.gitignore` | Git 忽略规则 |
| `LICENSE` | MIT 许可证 |
| `README.md` | 项目说明文档 |
| `data.db` | SQLite 数据库（运行时自动生成） |
| `pyproject.toml` | Python 项目元数据和依赖配置 |

---

## 三、插件核心代码结构

```
nonebot_plugin_uppic/
├── __init__.py          # 主入口 · 事件处理器 · 核心业务逻辑
├── config.py            # 配置管理 · 指令配置文件读写
├── compress.py          # 图片压缩 · 感知哈希计算
├── ali_oss.py           # 阿里云 OSS 对象存储上传
├── web.py               # 静态网页生成器
└── drivers/
    └── fastapi.py       # FastAPI 驱动的静态文件路由注册
```

---

## 四、各模块详细功能

### 4.1 主入口 - `__init__.py`

**核心职责**：插件启动初始化、事件监听、指令处理

**实现的功能**：

| 功能模块 | 触发方式 | 权限 | 说明 |
|----------|----------|------|------|
| **随机发图** | 群聊发送指令名 | 群员 | 根据指令随机发送一张图片 |
| **添加图片** | `添加<指令>` | 群管/群主/授权用户 | 存储图片到对应指令文件夹，自动压缩和去重 |
| **删除图片** | `删除图片<指令>` | 群管/群主 | 分页预览并删除指定指令下的图片 |
| **禁用指令** | `@bot 禁用<指令>` | 群管/群主 | 分群禁用指定指令 |
| **启用指令** | `@bot 启用<指令>` | 群管/群主 | 恢复已禁用的指令 |
| **上传OSS** | `上传oss` | 群管/群主 | 生成静态网页并上传至阿里云 OSS |
| **上传权限** | `@bot 上传权限 <群号> <模式>` | 超级用户 | 设置群上传权限模式 |
| **添加上传用户** | `@bot 添加上传用户 <群号> <用户ID>` | 超级用户 | 为指定群友授予上传权限 |
| **移除上传用户** | `@bot 移除上传用户 <群号> <用户ID>` | 超级用户 | 移除指定群友的上传权限 |

**关键流程**：
- `driver.on_startup`：启动时检查文件夹、连接数据库、创建表结构
- `create_dir()`：为每个指令创建文件夹和数据库表，同步图片到数据库
- `web_app_init()`：初始化静态网页服务（FastAPI）

**关键代码**：
- 图片发送逻辑：`pic()` 函数，通过 SQL `ORDER BY RANDOM()` 随机选择图片
- 图片添加逻辑：`add_pic()` 函数，包含下载、压缩、去重、保存完整流程
- 权限检查：`_can_upload()` 函数，支持超级用户、群管、授权用户三级权限
- 指令管理：`handle_disable()` / `handle_enable()` 实现分群禁用/启用
- 删除图片：`handle_delete_pic()` / `process_delete_input()` 分页预览并删除图片

---

### 4.2 配置管理 - `config.py`

**核心职责**：读取配置、管理指令列表、处理配置文件持久化

**实现的功能**：

| 功能 | 说明 |
|------|------|
| **配置类定义** | `Config` 类定义了所有可配置项（存储路径、OSS 参数等） |
| **指令配置加载** | 从 `uppic_commands.json` 读取指令列表，支持旧格式迁移 |
| **配置持久化** | 修改指令后自动写回 JSON 文件 |
| **格式兼容** | 自动将旧版数组格式迁移为新版字典格式 |
| **完整性校验** | 检查指令名称合法性（仅允许字母、汉字、数字） |

**核心数据结构**：
```python
# 指令配置：{指令名 -> 该指令被禁用的群号集合}
uppic_commands_config: Dict[str, Set[int]]
```

**配置项说明**：

| 配置项 | 必填 | 默认值 | 说明 |
|--------|------|--------|------|
| `uppic_store_dir_path` | 否 | `get_data_dir("nonebot_plugin_uppic")` | 图片存储路径 |
| `uppic_banner_group` | 否 | `[]` | 禁用发图功能的群聊列表 |
| `uppic_endpoint` | 否 | `None` | 阿里云 OSS 自定义域名 |
| `uppic_bucket` | 否 | `None` | 阿里云 OSS 存储空间名称 |
| `uppic_region` | 否 | `None` | 阿里云 OSS bucket 所在地域 |
| `uppic_oss_access_key_id` | 否 | `None` | 阿里云用户 AccessKey ID |
| `uppic_oss_access_key_secret` | 否 | `None` | 阿里云用户 AccessKey Secret |
| `uppic_oss_no_upload_list` | 否 | `[]` | 不上传到 OSS 的指令列表 |
| `uppic_super_users` | 否 | `[]` | 超级用户列表（可跨群管理权限） |

**权限配置文件**：`uppic_permissions.json`

```json
{
  "123456789": {
    "mode": "admin_only",
    "allowed_users": [10001, 10002]
  }
}
```

| 字段 | 说明 |
|------|------|
| `mode` | 权限模式：`admin_only`（仅群管）或 `all_members`（所有群员） |
| `allowed_users` | 超级用户单独授权的用户ID列表 |

---

### 4.3 图片压缩 - `compress.py`

**核心职责**：图片压缩、格式转换、感知哈希计算（去重）

**实现的功能**：

| 功能 | 说明 |
|------|------|
| **GIF 压缩** | 动态 GIF 按帧跳过、缩放、减色逐步压缩，保留动画效果 |
| **静态图片压缩** | JPEG/PNG 等转为 JPEG，限制最大尺寸和质量 |
| **自适应压缩** | 根据图片类型自动选择压缩策略，控制在 1MB 以内 |
| **格式检测** | 根据图片内容判断扩展名（不依赖 URL） |
| **感知哈希** | 计算 pHash，用于相似图片检测 |

**去重原理**：计算新图片与数据库中已有图片的 pHash 汉明距离，≤5 认为相似，拒绝重复添加。

**关键函数**：

| 函数 | 说明 |
|------|------|
| `compress_image_from_bytes()` | 统一入口，调用自适应压缩 |
| `adaptive_compress_bytes()` | 根据图片类型选择压缩策略 |
| `_compress_gif_bytes()` | GIF 专用压缩，保留动画 |
| `_compress_static_bytes()` | 静态图片压缩，转为 JPEG |
| `compute_phash()` | 计算图片感知哈希值 |
| `get_image_extension()` | 根据图片内容获取正确扩展名 |

---

### 4.4 阿里云 OSS - `ali_oss.py`

**核心职责**：文件上传至阿里云 OSS 对象存储

**实现的功能**：

| 功能 | 说明 |
|------|------|
| **单文件上传** | `upload_file()` 异步上传单个文件 |
| **文件夹上传** | `upload_folder()` 并发上传整个文件夹 |
| **文件删除** | `delete_file()` 异步删除 OSS 上的文件 |
| **存在性检查** | `check_file_exists()` 检查 OSS 中是否已存在文件 |
| **配置校验** | 检测 OSS 参数是否完整，决定是否启用 OSS 功能 |

**使用场景**：服务器配置较低或无公网 IP 时，通过 OSS 搭建静态网页供用户查看图片。

**关键类**：`OSSUploaderV2`

---

### 4.5 静态网页生成 - `web.py`

**核心职责**：生成静态图片库网站

**实现的功能**：

| 功能 | 说明 |
|------|------|
| **主页生成** | 展示所有指令文件夹列表和图片数量 |
| **文件夹页面** | 展示该指令下所有图片的缩略图和查看链接 |
| **完整站点生成** | `generate_static_site()` 生成整个静态网站 |
| **增量更新** | `generate_command_html()` 仅更新指定指令的页面 |

**页面结构**：
- 首页：文件夹卡片列表，显示每个指令的图片数量
- 子页面：图片网格展示，支持懒加载和点击查看原图

**关键类**：`StaticImageGalleryGenerator`

---

### 4.6 FastAPI 驱动 - `drivers/fastapi.py`

**核心职责**：注册静态文件路由

**实现的功能**：
- 将 `public` 目录挂载到 `/uppic` 路径，使生成的静态网页可通过浏览器访问

**访问地址**：`http://<host>:<port>/uppic/`

---

## 五、运行时文件结构

```
uppic/                    # 存储根目录（可配置）
├── img/                    # 图片文件夹
│   ├── capoo/              # 指令名文件夹
│   │   ├── uppic_capoo_0000000001.jpg
│   │   └── uppic_capoo_0000000002.jpg
│   └── 马哥/
│       └── ...
├── database/
│   └── data.db             # SQLite 数据库
├── public/                 # 静态网页生成目录
│   ├── index.html
│   ├── capoo/
│   │   ├── index.html
│   │   └── *.jpg
│   └── ...
├── uppic_commands.json   # 指令配置文件
└── uppic_permissions.json # 上传权限配置文件
```

---

## 六、数据库表结构

### 6.1 `folder_snapshot` 表

| 字段 | 类型 | 说明 |
|------|------|------|
| `command` | TEXT | 指令名，主键 |
| `folder_mtime` | REAL | 文件夹修改时间戳 |
| `checked_at` | REAL | 上次检查时间 |

### 6.2 `Pic_of_<指令名>` 表（动态创建）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | INTEGER | 自增主键 |
| `img_url` | TEXT | 图片相对路径 |
| `phash` | TEXT | 图片感知哈希值 |

---

## 七、核心工作流程

### 7.1 启动流程

```
加载配置 → 连接数据库 → 创建文件夹 → 创建表结构 → 同步图片到数据库 → 初始化网页服务
```

### 7.2 触发指令发图

```
匹配指令 → 检查是否被禁用 → 查询数据库随机图片 → 发送图片
```

### 7.3 添加图片

```
下载图片 → 压缩 → 计算 pHash → 去重检查 → 保存文件 → 写入数据库 → 上传 OSS(可选)
```

### 7.4 上传 OSS

```
生成静态网页 → 复制图片 → 批量上传至阿里云 OSS
```

### 7.5 删除图片

```
输入指令 → 选择页码 → 查看图片预览 → 选择序号删除 → 删除本地文件 → 删除数据库记录 → 删除 OSS 文件(可选) → 更新静态网页(可选)
```

---

## 八、技术栈

| 技术 | 用途 |
|------|------|
| NoneBot 2 | 聊天机器人框架 |
| OneBot V11 | QQ 协议适配器 |
| SQLite (aiosqlite) | 异步数据库操作 |
| Pillow | 图片处理 |
| imagehash | 感知哈希计算 |
| alibabacloud_oss_v2 | 阿里云 OSS SDK |
| FastAPI | 静态文件服务（可选） |

---

## 九、指令使用说明

### 9.1 基础指令

| 指令 | 权限 | 需要@ | 范围 | 说明 |
|------|------|-------|------|------|
| `<指令名>` | 群员 | 否 | 群聊 | 随机发送一张对应指令的图片 |
| `添加<指令>` | 群管/群主/授权用户 | 否 | 群聊 | 让 bot 存储图片到对应文件夹下 |
| `删除图片<指令>` | 群管/群主 | 否 | 群聊 | 分页预览并删除指定指令下的图片 |
| `@bot 禁用<指令>` | 群管/群主 | 是 | 群聊 | 在本群禁用该指令 |
| `@bot 启用<指令>` | 群管/群主 | 是 | 群聊 | 恢复本群被禁用的指令 |
| `上传oss` | 群管/群主 | 否 | 群聊 | 生成静态网页并上传至 OSS |

### 9.2 超级用户指令

| 指令 | 权限 | 需要@ | 说明 |
|------|------|-------|------|
| `@bot 上传权限 <群号> admin_only` | 超级用户 | 是 | 设置该群仅群管可上传 |
| `@bot 上传权限 <群号> all_members` | 超级用户 | 是 | 设置该群所有群员可上传 |
| `@bot 添加上传用户 <群号> <用户ID>` | 超级用户 | 是 | 为指定群友授予上传权限 |
| `@bot 移除上传用户 <群号> <用户ID>` | 超级用户 | 是 | 移除指定群友的上传权限 |

### 9.3 删除图片流程

```
发送「删除图片<指令>」→ 输入页码 → 查看该页图片 → 输入序号删除 → 删除完成
```

**示例**：
```
删除图片capoo
→ 删除图片「capoo」，共 12 张图片，共 3 页。请输入页码（1-3）
→ 2
→ === 第 2 页 / 共 3 页 ===
→ 序号 5: [图片5]
→ 序号 6: [图片6]
→ 序号 7: [图片7]
→ 序号 8: [图片8]
→ 请输入要删除的序号（5-8）
→ 6 8
→ 成功删除 2 张图片！
```

---

## 十、注意事项

1. **图片压缩**：大于 1MB 的图片会被压缩至 1MB 以内
2. **相似图片检测**：使用感知哈希算法，相似图片无法重复添加
3. **运行时修改**：不建议在 bot 运行期间直接修改图片文件夹内容
4. **OSS 配置**：需要完整配置 OSS 参数才能启用静态网页功能
5. **权限管理**：
   - 默认仅群管/群主可上传图片
   - 超级用户可跨群管理上传权限
   - 可通过配置为特定群开放全员上传或指定用户上传
6. **动图支持**：支持 GIF 动图上传，压缩时保留动画效果
7. **删除图片**：删除时会同步删除本地文件、数据库记录和 OSS 文件（如果已配置）
